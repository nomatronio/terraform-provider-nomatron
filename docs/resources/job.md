---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "nomatron_job Resource - nomatron"
subcategory: ""
description: |-
  Nomatron application job resource.
---

# nomatron_job (Resource)

Nomatron application job resource.

## Example Usage

```terraform
resource "nomatron_job" "example" {
  org_name          = "platform"
  app_slug          = "payments"
  name              = "Web"
  slug              = "web"
  jobspec_path      = "jobs/web.nomad.hcl"
  jobspec_type      = "hcl"
  repo_url          = "https://github.com/acme/payments-web"
  default_namespace = "payments"
  priority          = 1
}

resource "nomatron_job" "directory" {
  org_name         = "platform"
  app_slug         = "payments"
  name             = "Worker"
  slug             = "worker"
  jobspec_type     = "hcl"
  source_mode      = "directory"
  source_directory = "jobs/worker"
  job_file_path    = "jobs/worker/job.nomad.hcl"
  job_var_file_paths = [
    "jobs/worker/base.hcl",
    "jobs/worker/generated.json",
  ]
  default_namespace = "payments"
  priority          = 2
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `app_slug` (String) Application slug that owns the job.
- `jobspec_type` (String) Job spec source type.
- `name` (String) Job display name.
- `org_name` (String) Organization name that owns the application.

### Optional

- `cluster_id` (String) Optional cluster override for this job.
- `default_namespace` (String) Default Nomad namespace for this job.
- `description` (String) Job description.
- `job_file_path` (String) Optional repository-relative Nomad jobspec file within `source_directory`. Set this when directory discovery would be ambiguous.
- `job_var_file_paths` (List of String) Ordered repository-relative Nomad jobspec var files used only with HCL jobspecs. Entries may be HCL assignment files or JSON object files. Nomatron server-side source resolution applies these values; the provider does not read or convert local files.
- `jobspec_path` (String) Path to the job spec within the repository for file source mode. Directory mode uses `source_directory` as the canonical source path.
- `priority` (Number) Deployment group priority. Jobs with the same priority deploy together.
- `repo_url` (String) Optional repository URL override for this job. Omit to inherit the application repository.
- `slug` (String) Stable job slug. If omitted, the server generates one.
- `source_directory` (String) Repository-relative directory used when `source_mode` is `directory`.
- `source_mode` (String) Source shape for this job. Use `file` for the existing single jobspec path behavior or `directory` for a repository directory containing one Nomad jobspec and optional Nomad jobspec var files.

### Read-Only

- `created_at` (String) Timestamp when the job was created.
- `effective_repo_url` (String) Repository URL Nomatron will use for this job after applying application-level inheritance.
- `id` (String) Job ID.
- `is_primary` (Boolean) Whether this job is in the first deployment group.
- `updated_at` (String) Timestamp when the job was last updated.
